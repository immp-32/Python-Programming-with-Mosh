'''
tuple is immutable(i.e) we cannot append item in tuple
and replace the item.
tuple is only used to show the information.
'''

numbers = (1, 4, 6, 3, 6)
print(numbers[4])
print(numbers.index(3))
print(numbers.count(6))